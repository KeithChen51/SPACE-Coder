# BRD 决策台账 - <项目名称>

> 生成时间: YYYY-MM-DD HH:MM
> 最后更新: YYYY-MM-DD HH:MM
> 当前轮次: —
> 当前阶段: B
> project_slug: <slug>
> Skill: brd-writer

<!--
"当前阶段"取值与更新时机：
  B     — 台账刚创建，Phase B 诊断进行中
  C     — 进入 Phase C 收敛（含 Phase D.5 回退后重新收敛）
  D.5   — P0 确认率 100%，前提挑战进行中
  E     — 前提挑战通过，充分性判定进行中
  E.5   — 充分性通过，终稿前确认进行中
  F     — 终稿输出中
  DONE  — 终稿已落盘

每次 Phase 切换时同步更新此标记。跨会话恢复时直接读取，不需要从字段状态反推。
-->

## 1. P0 字段确认状态

> 所有 P0 字段的唯一权威源。`展开状态`、`只看缺口`、`生成终稿前摘要` 均从本节读取。
>
> Phase A 元字段（#1 项目类型）锁定后，根据项目类型从 `references/p0-fields.md` 加载追加字段到 §1.2，页面定位字段按需展开 §1.3。
>
> **"方法论依据"列填写规则**：`[决策]` 型字段必须填写方法论名称及关键推导（如 `JTBD → 场景匹配`）；`[事实]` 型字段填写信息来源（如 `来源: project-profile` / `需求方确认` / `数据佐证`）。字段分类见 `references/p0-fields.md`。

### 1.1 通用 P0

| # | 字段 | 值 | 状态 | 锁定轮次 | 方法论依据 |
|---|------|-----|------|---------|-----------|
| 1 | 项目类型 | — | open | — | — |
| 2 | 项目背景 | — | open | — | — |
| 3 | 利益相关角色 | — | open | — | — |
| 4 | 核心痛点 | — | open | — | — |
| 5 | 核心价值模型 | — | open | — | — |
| 6 | 范围定义 | — | open | — | — |
| 7 | 关键风险与兜底策略 | — | open | — | — |
| 8 | 项目周期 | — | open | — | — |

### 1.2 类型追加 P0

> Phase A 定性后，根据项目类型从 p0-fields.md 加载追加字段，追加到此节。
> 模板不预置具体行——由 AI 在 Phase A 完成后按实际类型填入。
>
> **条件字段过滤规则**：p0-fields.md 中标注"若涉及直接商业化"的字段，仅在 #3 为"是"时写入；标注"若涉及页面"的字段，仅在 #2 为"是"时写入。条件不满足的字段**不写入本节**——写入即意味着必须确认，避免产生永远 open 的伪缺口。

| # | 字段 | 值 | 状态 | 锁定轮次 | 方法论依据 |
|---|------|-----|------|---------|-----------|
| — | （Phase A 定性后展开） | — | — | — | — |

<!--
### 1.3 页面定位字段

由 buildFieldSet 派生：运营型恒展开，集成型恒不展开，其他类型默认展开（内部工具默认含页面）。
非页面项目不展开——避免 `只看缺口` 产生伪缺口。

| # | 字段 | 值 | 状态 | 锁定轮次 | 方法论依据 |
|---|------|-----|------|---------|-----------|
| 1 | 项目覆盖对象 | — | open | — | — |
| 2 | 各端目标用户 | — | open | — | — |
| 3 | 各端主要用途 | — | open | — | — |
| 4 | 页面定位判断（操作/配置/查看） | — | open | — | — |
| 5 | 下游待确认边界 | — | open | — | — |
-->

## 2. 冲突记录

> 当 Phase D 检测到已锁定字段之间存在矛盾时，记录在此。解决后标记 resolved。

| # | 冲突字段 | 冲突描述 | 状态 | 解决方式 | 解决轮次 |
|---|---------|---------|------|---------|---------|
| — | — | — | — | — | — |

## 3. 轮次变更日志

> 每轮 Phase D 锁定后追加一条记录。Phase B 批量锁定和回滚时也在此记录。
> 本节初始为空——无记录表示尚未进入 Phase B 台账动作或 Phase C/D。

<!--
=== Phase C/D 逐题锁定记录格式 ===

### Round <n>
- **时间**: YYYY-MM-DD HH:MM
- **动作**: （锁定 / 修改）
- **变更字段**:
  - `<字段名>`: <旧值>(<旧状态>) → <新值>(<新状态>)
- **方法论依据**: 决策型字段填方法论；事实型字段填信息来源
- **需求方原话摘要**: （一句话，用于追溯）

变更字段格式说明：
- 首次锁定：`核心痛点`: —(open) → 门店巡检漏检率高(locked)
- 修改已锁定字段：`核心痛点`: 门店巡检漏检率高(locked) → 巡检+异常上报双痛点(locked)
- 回滚时按此记录反转恢复。

=== Phase B 批量锁定记录格式 ===

### Round <n>（Phase B 批量锁定）
- **时间**: YYYY-MM-DD HH:MM
- **动作**: Phase B 批量锁定
- **变更字段**:
  - `项目背景`: —(open) → 门店巡检数字化转型(locked)
  - `利益相关角色`: —(open) → 区域经理/巡检员/店长(locked)
  - `核心痛点`: —(open) → 巡检漏检率高+异常上报滞后(locked)
- **信息来源**: project-profile + 需求方确认
- **冲突检测**: 无冲突 / 发现冲突（见 §2 #<n>）
- **需求方原话摘要**: （一句话，用于追溯）

回滚记录格式：

### Round <n>（回滚）
- **时间**: YYYY-MM-DD HH:MM
- **动作**: 回滚 Round <m>
- **变更字段**:
  - `<字段名>`: <当前值>(<当前状态>) → <回滚后的值>(<回滚后的状态>)
- **原因**: （需求方要求 / 冲突解决）
-->

## 4. 充分性门槛快照

> Phase E 每次执行后更新本节。全部通过后才允许进入 Phase F。

| 门槛 | 适用 | 状态 | 备注 |
|------|------|------|------|
| 字段完备门（P0 确认率 100%） | 所有类型 | — | — |
| 一致性门（冲突字段数 0） | 所有类型 | — | — |
| 度量门（目标可衡量） | 所有类型，检查口径随类型变化 | — | — |
| 范围门（范围定义明确） | 所有类型 | — | — |
| 方法论门（P0 决策有方法论映射） | 所有类型 | — | — |
| 角色门（角色已识别、痛点已明确） | 所有类型 | — | — |
| 页面门（定位字段完整） | 项目含页面时 | — | — |
