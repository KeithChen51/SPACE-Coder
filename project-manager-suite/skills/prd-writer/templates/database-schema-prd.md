# PRD 全局应用：Database Schema

<!--
  本文件是数据库表结构 PRD 模板。{} 中的内容为占位符，需替换为实际内容。
  <!-- 注释 --> 中的内容为写作指引，完成后删除。

  ─── 文档定位 ────────────────────────────────────────────────────
  本文档是全系统数据库表结构的**唯一权威源**。
  - 其他 PRD 引用表结构时应链接至本文档对应章节，不重复定义字段
  - 子 PRD 的数据链路表中"数据源"列应引用本文档的表.字段
  - 主文档附录 B 从本文档提取摘要，不做展开

  ─── 开写前必做 ────────────────────────────────────────────────────
  1. 读主文档 (docs/PRD/prd-主文档.md)，理解系统架构和数据全景
  2. 查看实际数据库 schema（DDL / migration / Prisma schema / 初始化 SQL）
  3. 读所有已完成的子 PRD，梳理每张表被哪些接口使用
  4. 按数据源对表进行分组（见下方分组原则）

  ─── 与主文档附录 B 的关系 ────────────────────────────────────────
  主文档附录 B 是"全表摘要"（表名 + 一句话用途 + 数据来源），本文档是"完整定义"。
  两者保持一致，但本文档是权威源，冲突时以本文档为准。
-->

> **文档版本**: {版本号} | **最后更新**: {日期}
> **关联文档**: [prd-主文档.md](prd-主文档.md) · {其他关联 PRD 链接} · [术语表](prd-全局应用-术语表.md)

**定位**：本文档是全系统数据库表结构的**唯一权威源**。其他 PRD 引用表结构时应链接至本文档对应章节，不重复定义字段。

**数据库**：`{数据库名}`

---

## §1 全表总览

<!--
  总览表是全文的索引，读者一眼看清所有表的状态和归属。

  ─── 状态标记规则 ────────────────────────────────────────────────
  - `已有`：当前数据库已存在，字段定义与目标态一致，无需变更
  - `新建`：目标态新建的表，当前数据库不存在
  - `变更`：已有表需重命名、增删字段或调整结构

  ─── 所属方规则 ────────────────────────────────────────────────
  所属方 = 数据的写入方（谁往表里写数据）：
  - 管理台配置 → 运营通过管理台维护
  - {外部系统名} 同步 → 同步服务从外部系统拉取写入
  - 服务端 → 服务端自管（聚合计算 / 日志）

  ─── 定义于 列 ────────────────────────────────────────────────
  使用 Markdown 锚点链接跳转到对应章节。
  锚点格式：`[§N.M](#nm-表名--中文名)`（注意 `--` 是两个连字符）

  ─── 排列顺序 ────────────────────────────────────────────────
  按 §2-§N 的分组顺序排列，组内按功能相关性排列。
-->

> **状态标记**：`已有` = 当前数据库已存在，无需变更 | `新建` = 目标态新建 | `变更` = 已有表需重命名或增删字段

| # | 表名 | 状态 | 所属方 | 作用 | 定义于 |
|---|------|------|-------|------|--------|
| 1 | `{表名}` | {已有/新建/变更} | {所属方} | {一句话作用} | [§N.M](#锚点) |

---

<!--
  ==========================================
  §2~§N 按数据源分组
  ==========================================

  ─── 分组原则（核心）────────────────────────────────────────────

  每个 § 章节对应**严格一个数据源/所有者**，绝不混合不同来源的表。

  判断标准：这些表的数据是谁写入的？
  - 同一个外部系统同步的表 → 放同一组
  - 不同外部系统 → 即使业务相似也必须分开（如"智慧营销"和"用车商城"不能合并为"营销数据类"）
  - 服务端自管的表（聚合 / 缓存 / 日志）→ 与外部数据源分开

  常见分组模式：
  - §2 配置管理类（管理台运营维护）
  - §3 {外部系统A} 类（如 BSS 同步）
  - §4 {外部系统B} 类（如智慧营销同步）
  - ...
  - §N 同步基础设施类（服务端自管）

  ─── 每组结构 ────────────────────────────────────────────────

  ## §N {数据源名}类

  > {一句话说明数据来源和读写方向}

  ### N.M `表名` — 中文名（状态后缀）

  > 状态：**{状态}** | 引用：[超链]

  {行为描述}

  | 字段表格 |

  **使用接口**：
  - 列表

  ---

  ─── 每张表的写作规范（详见下方模板）────────────────────────────
-->

## §2 {分组名称}类

<!--
  每个分组的开头用 blockquote 说明数据来源和读写方向。
  示例：
  > 由运营人员通过管理台维护，服务端实时读取用于规则计算。
  > 从 BSS 系统同步的业务数据，由同步服务定时写入。服务端只读不写（同步服务除外）。
-->

> {一句话说明数据来源和读写方向}

### 2.1 `{表名}` — {中文名}

<!--
  ─── 表头部分 ────────────────────────────────────────────────

  1. 状态行（必写）：
     > 状态：**已有** | 引用：[prd-区块X-名称 §Y](prd-区块X-名称.md)

     引用 = 哪个子 PRD 的哪个章节使用了这张表。
     多个引用用 `、` 分隔。

  2. 变更说明（仅变更状态的表写）：
     > **变更说明**：{简述变更内容}

  3. 行为描述（如有）：
     说明表的行数约束、分组规则等。
     示例：
     - 固定 5 行（NOT_DUE / DUE / ...），不可增删行。
     - 按 parentStatus 分组，每组按 sortOrder 排序，命中第一条即返回。
     - 支持完整 CRUD，运营可增删条目。
-->

> 状态：**{已有/新建/变更}** | 引用：[{PRD名} §{章节}]({PRD文件名}.md)

{行为描述（如"固定 N 行"、"按 X 分组"、"支持 CRUD"等）}

<!--
  ─── 字段表格 ────────────────────────────────────────────────

  根据表的状态选择对应的表格格式：

  ① 已有表（无变更）：标准 6 列
  | 字段 | 类型 | 可空 | 键 | 默认值 | 说明 |

  ② 变更表（有增删字段）：加「变更」列
  | 字段 | 类型 | 可空 | 键 | 默认值 | 变更 | 说明 |
  - 新增字段：变更列填 **新增**
  - 删除字段：字段名和类型用 ~~删除线~~，变更列填 **删除**，其他列留空
  - 未变字段：变更列留空

  ③ 聚合同步表（多数据源写入）：加「同步来源」+「同步周期」列
  | 字段 | 类型 | 可空 | 键 | 默认值 | 说明 | 同步来源 | 同步周期 |

  选择标准：
  - 表的状态为"变更"且有增删字段 → 用②
  - 表是聚合多个外部源的同步产物 → 用③
  - 其他 → 用①

  ─── 字段表格通用规则 ────────────────────────────────────────
  - 类型用 MySQL 类型写法（varchar(191)、int、tinyint(1)、json、text、datetime(3)、decimal(10,2) 等）
  - 可空：NO / YES
  - 键：PRI = 主键、UNI = 唯一索引、MUL = 普通索引，无索引则留空
  - 默认值：精确到实际值（如 0、NULL、CURRENT_TIMESTAMP(3)、auto_increment），无默认值则留空
  - 说明：一句话解释字段业务含义。**枚举型字段必须在说明中穷举所有合法值**，用括号列出（如"动力类型（EV / PHEV）"、"订单状态（进行中 / 已完成 / 已取消）"）。未穷举枚举值会导致下游 PRD、测试用例和代码杜撰不存在的值（见 anti-patterns.md #22）
-->

| 字段 | 类型 | 可空 | 键 | 默认值 | 说明 |
|------|------|------|-----|--------|------|
| {字段名} | {类型} | {NO/YES} | {PRI/UNI/MUL/空} | {默认值} | {说明} |

<!--
  ─── 索引（如有额外索引）────────────────────────────────────
  如果表有复合索引或字段表格中 PRI/UNI/MUL 列无法完整表达的索引，
  用 blockquote 单独列出：
  > 索引：`UNIQUE INDEX idx_vin_phone (vin, phone)`
-->

<!--
  ─── 使用接口（必写）────────────────────────────────────────
  列出所有读写这张表的接口。格式统一为：
  - `{METHOD} /api/{path}` — {一句话角色}（见 [{PRD名} §{章节}]({文件名}.md)）

  作用：
  1. 让开发者知道修改表结构会影响哪些接口
  2. 让 AI 能追溯表→接口→PRD 的完整链路

  ❌ 错误：只写接口路径，不写角色和 PRD 链接
  ✅ 正确：路径 + 一句话角色 + 超链接到定义该接口的 PRD 章节
-->

**使用接口**：
- `{METHOD} /api/{path}` — {一句话角色}（见 [{PRD名} §{章节}]({文件名}.md)）

---

### 2.2 `{表名}` — {中文名}（{状态后缀}）

<!--
  变更表示例：有增删字段时的写法
-->

> 状态：**变更** | 引用：[{PRD名} §{章节}]({PRD文件名}.md)

> **变更说明**：{简述变更内容，如"删除 `isActive`（原因）"或"新增 `store_name`（依赖）"}

{行为描述}

| 字段 | 类型 | 可空 | 键 | 默认值 | 变更 | 说明 |
|------|------|------|-----|--------|------|------|
| {正常字段} | {类型} | {NO/YES} | {键} | {默认值} | | {说明} |
| {新增字段} | {类型} | {NO/YES} | {键} | {默认值} | **新增** | {说明} |
| ~~{删除字段}~~ | ~~{类型}~~ | | | | **删除** | {删除原因} |

**使用接口**：
- `{METHOD} /api/{path}` — {一句话角色}（见 [{PRD名} §{章节}]({文件名}.md)）

---

<!--
  ─── 后续分组 ────────────────────────────────────────────────
  按相同结构继续写 §3、§4... 每个分组对应一个数据源。

  常见分组示例（仅供参考，以实际数据源为准）：

  ## §2 配置管理类        — 管理台运营维护
  ## §3 BSS 业务数据类    — BSS 系统同步
  ## §4 智慧营销类        — 智慧营销平台同步
  ## §5 用车商城类        — 用车商城同步
  ## §6 精诚长联类        — 精诚长联系统同步
  ## §7 迪粉汇类          — 迪粉汇同步
  ## §8 同步基础设施类    — 服务端自管

  ─── 分组自检 ────────────────────────────────────────────────
  写完所有分组后，检查：
  1. 每个分组内的所有表，数据写入方是否完全一致？
  2. 是否有两个不同外部系统的表混在同一组？
  3. 服务端自管的表（缓存/聚合/日志）是否与外部数据源分开？
  4. §1 总览表的分组归属和 §N 章节是否对应？
-->
