# PRD {N}: {区块名称}

<!--
  本文件是子文档模板。{} 中的内容为占位符，需替换为实际内容。

  ═══════════════════════════════════════════════════════════════
  开写前必做
  ═══════════════════════════════════════════════════════════════

  Step 1：确认边界 — 回答四个问题：
    a. 本区块管哪些 UI 元素？ → 决定"功能子区域"章节有几个
    b. 本区块用哪些数据字段？ → 决定"数据链路"章节列什么
    c. 本区块调哪些接口？ → 从 foundation-api 中查找对应接口
    d. 接口请求参数从哪来？ → 宿主 App 登录态 / 前序接口返回 / 全局状态 / 用户输入
       跨区块依赖必须写明"来自 PRD N 的哪个接口/状态"

  边界判断：如果某个字段/接口/管理页"主要服务于其他区块"，
  即使本区块顺带用到，也不要放在本 PRD——在数据链路表中引用即可。

  Step 2：阅读页面代码 — 读取该区块涉及的前端 Vue 3 组件文件：
    - 页面组件(.vue) + composable(.js) + 服务层(services)
    - 管理台页面组件(.vue)（如果有属于本区块的管理页）

  Step 3：阅读 foundation 产物 — 查找本区块涉及的：
    - foundation-glossary 中的术语
    - foundation-schema 中的表结构
    - foundation-api 中的接口定义

  Step 4：按下方模板结构撰写

  Step 5：边界自检 — 写完后按 references/anti-patterns.md 逐项检查

  ═══════════════════════════════════════════════════════════════
  关键规则
  ═══════════════════════════════════════════════════════════════

  - 术语必须使用 foundation-glossary 中的定义
  - Schema/API 信息只引用 foundation 产物，不重新定义
  - 接口契约引用 foundation-api 中的定义，子 PRD 只补充本区块的业务说明
  - 数据链路表中的"来源表.列"必须在 foundation-schema 中存在
-->

> **文档版本**: 1.0 | **最后更新**: {日期}
> **关联文档**: [主文档](prd-main-{slug}.md) · {相邻区块 PRD 链接} · [术语表](foundation-glossary-{slug}.md) · [Schema](foundation-schema-{slug}.md) · [API](foundation-api-{slug}.md)

---

## §1 文档范围

<!--
  一句话说清覆盖什么，括号内列出关键词。
  如果功能子区域各自包含管理台配置，在范围描述中体现。
-->

本文档覆盖**{区块名称}**（{关键词1} + {关键词2} + {关键词3}）。

### 需求清单

<!--
  在进入细节之前，先列出本区块要交付的全部需求。
  作用：
  - AI 快速建立上下文
  - 验收标准有锚点，每条需求对应验收用例
  - 范围变更可追溯
-->

| # | 需求 | 需求简述 | 对应章节 |
|---|------|---------|---------|
| R1 | {能力名称} | {一句话说明} | §X |
| R2 | {能力名称} | {一句话说明} | §Y |

---

## §2 页面整体布局

<!--
  用 ASCII 图标注本区块在页面中的位置。
  规则：
  - 只标注本文档覆盖的区域（← 本文档 §X-§Y）
  - 其他区域保留名称，不标注其他 PRD 的具体章节号
-->

{端名} 页面从上到下分为 {N} 个区域，纵向滚动：

```
┌─────────────────────────────┐
│ {区域A}                      │
├─────────────────────────────┤
│ {区域B}                      │  ← 本文档 §X-§Y
├─────────────────────────────┤
│ {区域C}                      │
└─────────────────────────────┘
```

---

## §3 {区块名}信息架构

<!--
  可选章节。当区块内部有多个展示模式或多层结构时使用。
  简单区块（只有单一模式、2-3 个子区域）可省略此章节，直接进入功能子区域。

  何时需要：
  - 区块有 2+ 种展示模式（如日常模式 / 编辑模式）
  - 区块内部有 4+ 层结构
  - 不同模式下展示的层级不同
-->

{区块名}由 **{N} 层**纵向排列组成。根据 {切换条件} 分为 **{模式A}** 和 **{模式B}** 两种展示分支：

```
{模式A}                                {模式B}

┌─────────────────────────────┐    ┌─────────────────────────────┐
│ ① {层1}           §X       │    │ ① {层1}           §X       │
├─────────────────────────────┤    ├─────────────────────────────┤
│ ② {层2}           §Y       │    │ ② {层2}           §Y       │
├─────────────────────────────┤    ├─────────────────────────────┤
│ ③ {层3}           §Z       │    │ ③ {替代层}        §W       │
└─────────────────────────────┘    └─────────────────────────────┘
```

### 3.1 模式切换规则

| 条件 | 模式 | 展示层 |
|------|------|--------|
| {条件1} | {模式A} | {展示哪些层} |
| {条件2} | {模式B} | {展示哪些层} |

### 3.2 全链路数据流

```
C端请求 {METHOD} /api/{path}({参数})    ← {参数来源说明}
                    ↓
          服务端 {ServiceName}
          ├── 读 {数据源表}（原始数据）
          ├── 读 {配置表}（配置数据）
          └── 规则引擎实时计算
              ├── {子计算1} → {输出字段1}
              ├── {子计算2} → {输出字段2}
              └── {子计算3} → {输出字段3}
                    ↓
          返回 display-ready JSON → C端直接渲染
```

---

<!--
  ==========================================
  §4~§N 功能子区域
  ==========================================

  每个功能子区域是一个【自包含模块】，子节按以下固定顺序排列：

  X.1 用户体验        — 数据来源声明 + ASCII 布局 + 交互说明 + 前端职责声明
  X.2 服务端处理逻辑  — 步骤列表 + 处理输入表（简单直透型可省略）
  X.3 数据链路        — 端到端追溯表（UI ← API ← 服务端 ← 数据源/配置源）
  X.4 异常与兜底      — 服务端兜底 + 前端渲染兜底 两个子表格
  X.5 管理台配置（如有）— 菜单/路径 + 配置字段表 + Admin API 引用 + 存储表引用

  顺序逻辑：
  UX → 服务端逻辑 → 数据链路 → 异常 → 配置
  = 用户看到什么 → 服务端怎么算 → 数据从哪来 → 出错怎么办 → 运营怎么改

  可省略的子节：
  - X.2 服务端处理逻辑：简单直透型（单表查询直接返回，无计算）可省略
  - X.5 管理台配置：该子区域无配置页时省略

  模块化原则：
  管理台配置跟着功能子区域走，不集中放末尾。
  AI 实现某个功能时，只需读该章节就能获取完整上下文。
-->

## §X {功能子区域1}

### X.1 用户体验

<!--
  必须包含三部分：
  1. 数据来源声明（引用 foundation-api 中的接口）
  2. ASCII 布局图
  3. 前端职责声明（防止 AI 在前端写多余业务逻辑）
-->

**数据来源**：`{METHOD} /api/{path}` 返回的 `{字段名}`（含 {子字段列表}）
> 接口详情见 [foundation-api-{slug}.md §N.M](...)

**布局**：

```
{ASCII 布局图}
```

**前端职责**：{仅渲染 xxx 字段，不做 yyy 判断/推导}

### X.2 服务端处理逻辑

<!--
  描述服务端"怎么算"的步骤。与数据链路表的"数据从哪来"互补。

  何时需要：
  - 服务端有计算/匹配/拼接/判定逻辑 → 必写
  - 简单直透（单表查询直接返回，无计算） → 可省略此节
-->

服务端按以下步骤构建 `{输出字段}`：

1. {步骤1：从哪里查什么数据}
2. {步骤2：按什么规则计算/匹配/拼接}
3. {步骤3：返回什么结果}

**处理输入**：

| 输入 | 来源 | 说明 |
|------|------|------|
| {输入变量} | {`表名` WHERE `条件`} | {说明} |

### X.3 数据链路

<!--
  来源必须追溯到底：精确到 foundation-schema 中的具体表.字段。
  不能只写"服务端计算"。

  表头固定 5 列：UI 元素 / API 字段 / 计算规则 / 数据源（服务端读取） / 配置源（服务端读取）
  数据源和配置源中的表名必须在 foundation-schema 中存在。
-->

> 下表为端到端追踪：UI 元素 ← API 字段 ← 服务端计算 ← 数据源/配置源。**数据源和配置源均由服务端读取**，前端不直接访问。

| UI 元素 | API 字段 | 计算规则 | 数据源（服务端读取） | 配置源（服务端读取） |
|---------|----------|---------|--------|--------|
| {元素} | `{字段}` | {规则或"直透"} | {`表.字段`} | {`配置表.字段` 或 —} |

### X.4 异常与兜底

<!--
  必须区分服务端兜底和前端渲染兜底，分两个子表格写。
-->

**服务端兜底**：

| 场景 | 处理 |
|------|------|
| {数据缺失/查询失败场景} | {返回默认值或空集合} |

**前端渲染兜底**：

| 场景 | 处理 |
|------|------|
| {API 字段为 null/空场景} | {降级展示方式} |

### X.5 管理台配置：{配置页名称}

<!--
  如果该子区域有对应的管理台配置页，在这里描述。
  无配置页则删除此小节。

  接口和存储表引用 foundation-api 和 foundation-schema 中的定义。
-->

**菜单名称**：{侧边栏显示的中文名}
**所属菜单**：{一级菜单} › {二级菜单（如有）}
**路径**：`/{route}`

{一句话说明配置页功能}

| 字段 | 输入类型 | 约束 | 说明 |
|------|---------|------|------|
| {字段} | {输入类型} | {必选/最多N字 等} | {说明} |

**接口**（引用 foundation-api）：
- `GET /api/admin/{path}` — 获取配置（详见 [foundation-api §N.M](...)）
- `PUT /api/admin/{path}` — 保存配置（详见 [foundation-api §N.M](...)）

**存储**：`{表名}` 表（详见 [foundation-schema §N.M](...)）

---

## §Y {功能子区域2}

<!--
  重复上面的 5 子节结构。
-->

### Y.1 用户体验

### Y.2 服务端处理逻辑

### Y.3 数据链路

### Y.4 异常与兜底

---

## §M 异常与兜底策略（全局）

<!--
  可选章节。当本区块的核心接口是聚合型（读多个数据源），需要此章节。
  简单区块可省略。
-->

### M.1 接口级兜底

| 场景 | 处理 |
|------|------|
| `{METHOD} /api/{path}` 超时 | {全局降级展示} |
| 接口返回 `code ≠ 0` | {同上} |

### M.2 子数据源降级

| 子数据源 | 失败影响 | 降级策略 |
|---------|---------|---------|
| `{数据源表}` | 无法计算 {字段} | {该字段返回默认值，其他正常} |

---

## §M+1 接口契约

<!--
  完整接口契约定义在 foundation-api 中。子 PRD 引用而非复制。

  子 PRD 只补充本区块特有的业务说明：
  1. 本区域使用的全部响应字段列表（按功能子区域标注章节）
  2. 前端渲染约束
  3. 特殊矩阵/映射表

  Admin API 已在各功能子区域的 X.5 管理台配置小节中引用，此处不重复。
-->

### M+1.1 C 端接口：`{METHOD} /api/{path}`

{一句话说明接口用途}

> 完整接口契约见 [foundation-api-{slug}.md §N.M](...)。

**本区域业务说明**：

- **本区域使用的全部响应字段**：`{字段1}`（§X）、`{字段2}`（§Y）、`{字段3[]}`（§Z）
- **前端不做业务判断**：{所有判定/拼装/匹配逻辑均由服务端完成，前端仅渲染返回值}

---

## §M+2 验收标准

<!--
  §1 需求清单的每条需求（R1/R2/R3...）都必须有对应的验收用例。
  写完后回看需求清单逐条核对，确保无遗漏。

  4 类必覆盖场景：
  1. 业务规则（从 X.2/X.3 提取）
  2. UX 交互（从 X.1 提取）
  3. 管理台配置闭环（从 X.5 提取）
  4. 异常兜底（从 X.4 提取）

  边界约束：只验收本区块的功能，不验收其他区块的行为。
-->

### 业务规则

| # | 场景 | 输入条件 | 预期结果 |
|---|------|---------|----------|
| 1 | {正向场景} | {满足条件} | {预期结果} |
| 2 | {反向场景} | {不满足条件} | {预期结果} |

### UX 交互

| # | 场景 | 操作 | 预期结果 |
|---|------|------|----------|
| N | {交互场景} | {用户操作} | {预期结果} |

### 管理台配置闭环

| # | 场景 | 操作 | 预期结果 |
|---|------|------|----------|
| N | {改某类配置} | 管理台修改 {字段} | C 端 {对应变化} |

### 异常兜底

| # | 场景 | 输入条件 | 预期结果 |
|---|------|---------|----------|
| N | {数据缺失/查询失败} | {异常条件} | {降级表现} |
