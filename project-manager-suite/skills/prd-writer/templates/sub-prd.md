# PRD {N}: {区块名称}

<!--
  本文件是子文档模板。{} 中的内容为占位符，需替换为实际内容。
  <!-- 注释 --> 中的内容为写作指引，完成后删除。

  ═══════════════════════════════════════════════════════════════
  开写前必做
  ═══════════════════════════════════════════════════════════════

  Step 1：确认边界 — 回答四个问题：
    a. 本区块管哪些 UI 元素？ → 决定"功能子区域"章节有几个
    b. 本区块用哪些数据字段？ → 决定"数据链路"章节列什么
    c. 本区块调哪些接口？ → 决定"接口契约"章节写什么
    d. 接口请求参数从哪来？ → 宿主 App 登录态 / 前序接口返回 / 全局状态 / 用户输入
       跨区块依赖必须写明"来自 PRD N 的哪个接口/状态"

  边界判断：如果某个字段/接口/管理页"主要服务于其他区块"，
  即使本区块顺带用到，也不要放在本 PRD——在数据链路表中引用即可。

  Step 2：深度阅读代码 — 必须覆盖该区块涉及的所有文件：
    - C 端前端：组件(.vue) + composable(.js) + 适配器(adapters) + 服务层(services)
    - 服务端：Controller + Service + Entity/DTO + Mapper
    - 管理台：页面组件(.vue)（如果有属于本区块的管理页）
    - 数据库：涉及的表结构 + 初始化数据

  Step 3：按下方模板结构撰写

  Step 4：边界自检 — 写完后按 SKILL.md Pre-Delivery Checklist 逐项检查
         反模式详见 references/anti-patterns.md
-->

> **文档版本**: 1.0 | **最后更新**: {日期}
> **关联文档**: [prd-主文档.md](prd-主文档.md) · {相邻区块 PRD 链接} · [术语表](prd-全局应用-术语表.md) · [数据库 Schema](prd-全局应用-database-schema.md) · [接口总览](prd-全局应用-api.md)
>
> ⚠️ **现状差异时效性声明**：本文档中的「现状差异」描述基于撰写时的代码快照，随开发推进可能已过时。AI 开发时应**以真实代码为准**，对比代码现状与本文档的**目标态需求**，自行判断差异并决定实现路径。

---

## §1 文档范围

<!--
  一句话说清覆盖什么，括号内列出关键词。
  如果功能子区域各自包含管理台配置，在范围描述中体现。
  示例：本文档覆盖**保养卡片区**（5 态状态判定 + ... + 车辆缓存数据页 + 同步状态页）。
         每个功能子区域自包含对应的管理台配置页和 Admin API。
-->

本文档覆盖**{区块名称}**（{关键词1} + {关键词2} + {关键词3}）。

### 需求清单

<!--
  在进入布局和实现细节之前，先列出本区块要交付的全部需求。
  作用：
  - AI 快速建立上下文，不用通读全文就知道要交付哪些能力
  - 验收标准有锚点，每条需求对应验收用例可交叉校验完整性
  - 范围变更可追溯，需求条目增删比散布在各子章节中更清晰

  格式：四列表格（# / 需求 / 需求简述 / 对应章节）
  - 需求：2-6 字的能力名称
  - 需求简述：一句话说明要做什么
  - 对应章节：指向本文档中详细描述该需求的章节号
-->

| # | 需求 | 需求简述 | 对应章节 |
|---|------|---------|---------|
| R1 | {能力名称} | {一句话说明} | §X |
| R2 | {能力名称} | {一句话说明} | §Y |

---

## §2 页面整体布局

<!--
  用 ASCII 图标注本区块在页面中的位置。
  规则：
  - 只标注本文档覆盖的区域（← 本文档 §X-§Y）
  - 其他区域保留名称，不标注其他 PRD 的具体章节号
  - 不要对不属于本区块的区域做额外说明
-->

C 端页面从上到下分为 {N} 个区域，纵向滚动：

```
┌─────────────────────────────┐
│ {区域A}                      │
├─────────────────────────────┤
│ {区域B}                      │  ← 本文档 §X-§Y
├─────────────────────────────┤
│ {区域C}                      │
└─────────────────────────────┘
```

<!--
  如果本区块有全局联动行为（如切换车辆后全页刷新），在这里简要说明。
  但只描述"本区块触发的联动"，不描述"其他区块如何响应"（那是别人的 PRD）。
-->

---

## §3 {区块名}信息架构

<!--
  可选章节。当区块内部有多个展示模式（如日常/维保中）或多层结构时使用。
  简单区块（只有单一模式、2-3 个子区域）可省略此章节，直接进入功能子区域。

  ─── 何时需要 ────────────────────────────────────────
  - 区块有 2+ 种展示模式（如日常模式 / 维保中模式）
  - 区块内部有 4+ 层结构，需要先展示整体再逐层细写
  - 不同模式下展示的层级不同（部分层被替换或隐藏）

  ─── 包含内容 ────────────────────────────────────────
  1. 双模式 / 多模式 ASCII 对比图：展示各模式下的层级差异
  2. 模式切换规则表：什么条件触发哪种模式
  3. 全链路数据流图：请求参数来源 → 服务端处理 → 子计算分支 → 返回字段
-->

{区块名}由 **{N} 层**纵向排列组成。根据 {切换条件} 分为 **{模式A}** 和 **{模式B}** 两种展示分支：

```
{模式A}                                {模式B}

┌─────────────────────────────┐    ┌─────────────────────────────┐
│ ① {层1}           §X       │    │ ① {层1}           §X       │
├─────────────────────────────┤    ├─────────────────────────────┤
│ ② {层2}           §Y       │    │ ② {层2}           §Y       │
├─────────────────────────────┤    ├─────────────────────────────┤
│ ③ {层3}           §Z       │    │ ③ {替代层}        §W       │
└─────────────────────────────┘    └─────────────────────────────┘
```

### 3.1 模式切换规则

| 条件 | 模式 | 展示层 |
|------|------|--------|
| {条件1} | {模式A} | {展示哪些层} |
| {条件2} | {模式B} | {展示哪些层} |

### 3.2 全链路数据流

<!--
  展示从请求到响应的完整数据流向。
  参考 PRD 2 §3.2 的格式：请求 → 服务端 Service → 各子计算分支 → 返回字段。
-->

```
C端请求 {METHOD} /api/{path}({参数})    ← {参数来源说明}
                    ↓
          服务端 {ServiceName}
          ├── 读 {数据源表}（原始数据）
          ├── 读 {配置表}（配置数据）
          └── 规则引擎实时计算
              ├── {子计算1} → {输出字段1}
              ├── {子计算2} → {输出字段2}
              └── {子计算3} → {输出字段3}
                    ↓
          返回 display-ready JSON → C端直接渲染
```

---

<!--
  ==========================================
  §4~§N 功能子区域
  ==========================================
  （如果使用了 §3 信息架构，功能子区域从 §4 开始。
    如果省略了 §3，功能子区域从 §3 开始。）

  每个功能子区域是一个【自包含模块】，子节按以下固定顺序排列：

  X.1 用户体验        — 数据来源声明 + ASCII 布局 + 交互说明 + 前端职责声明
  X.2 服务端处理逻辑  — 步骤列表 + 处理输入表（简单直透型可省略）
  X.3 数据链路        — 端到端追溯表（UI ← API ← 服务端 ← 数据源/配置源）
  X.4 异常与兜底      — 服务端兜底 + 前端渲染兜底 两个子表格
  X.5 管理台配置（如有）— 菜单/路径 + 配置字段表 + Admin API + 存储表

  ─── 为什么是这个顺序 ────────────────────────────────────────

  UX → 服务端逻辑 → 数据链路 → 异常 → 配置
  = 用户看到什么 → 服务端怎么算 → 数据从哪来 → 出错怎么办 → 运营怎么改

  开发者先理解目标（UX），再理解实现（逻辑+数据），最后处理边界（异常+配置）。

  ─── 哪些子节可以省略 ────────────────────────────────────────

  - X.2 服务端处理逻辑：简单直透型（数据从单表直透，无计算）可省略
  - X.5 管理台配置：该子区域无配置页时省略

  ─── 模块化原则 ────────────────────────────────────────────

  如果该子区域有对应的管理台配置页，就在 X.5 中描述配置能力和 Admin API。
  不要把管理台集中放到文档末尾。
  AI 实现某个功能时，只需读该章节就能获取：
  前端渲染 + 服务端逻辑 + 配置来源 + 管理台操作 + Admin API 的完整上下文。
-->

## §X {功能子区域1}

### X.1 用户体验

<!--
  用 ASCII 布局图展示 UI 结构。
  标注关键元素的位置、内容、交互行为。

  ─── 必须包含三部分 ────────────────────────────────────────

  1. **数据来源声明**（布局前）：
     调用哪个接口 + 本区域渲染用到的字段
     示例：**数据来源**：`GET /api/basic-info` 返回的 `badges[]`（含 name / color / popoverTitle）

  2. **ASCII 布局图**

  3. **前端职责声明**（布局后）：
     明确前端只做什么、不做什么。这是防止 AI 在前端写多余业务逻辑的关键约束。
     示例：**前端职责**：逐条渲染 `benefits[]`，不判断权益类型，不查配置表。

  ❌ 错误：只写布局图，不声明前端职责边界
  ✅ 正确：数据来源 + 布局 + 前端职责 三件套
-->

**数据来源**：`{METHOD} /api/{path}` 返回的 `{字段名}`（含 {子字段列表}）

**布局**：

```
{ASCII 布局图}
```

**前端职责**：{仅渲染 xxx 字段，不做 yyy 判断/推导}

### X.2 服务端处理逻辑

<!--
  描述服务端"怎么算"的步骤。与数据链路表的"数据从哪来"互补。

  ─── 何时需要 ────────────────────────────────────────
  - 服务端有计算/匹配/拼接/判定逻辑 → 必写
  - 简单直透（单表查询直接返回，无计算） → 可省略此节

  ─── 结构 ────────────────────────────────────────────
  1. 步骤列表：用编号描述服务端处理流程（1. 查xxx 2. 按xxx匹配 3. 返回）
  2. 如果是状态判定，用 mermaid flowchart 展示判定流程
  3. 如果有匹配规则，用表格展示条件→输出映射
  4. **处理输入表**：列出每个输入变量的来源

  ❌ 错误：只写"服务端计算后返回"，不写具体怎么算
  ✅ 正确：步骤 + 规则/模板表 + 处理输入表
-->

服务端按以下步骤构建 `{输出字段}`：

1. {步骤1：从哪里查什么数据}
2. {步骤2：按什么规则计算/匹配/拼接}
3. {步骤3：返回什么结果}

<!--
  如果是状态判定，用 mermaid flowchart 展示：
  ```mermaid
  flowchart TD
      A[进入判定] --> B{条件1？}
      B -- 是 --> C[结果A]
      B -- 否 --> D{条件2？}
  ```
-->

<!--
  如果有条件→输出映射，用表格展示：
  | 条件 | 输出 |
  | xxx | yyy |
-->

**处理输入**：

| 输入 | 来源 | 说明 |
|------|------|------|
| {输入变量} | {`表名` WHERE `条件`} | {说明（为空时的回退计算方式等）} |

### X.3 数据链路

<!--
  来源必须追溯到底：每个字段的数据来源都必须精确到具体表和字段，不能只写"服务端计算"。
  AI 读 PRD 实现功能，来源不清楚 = 实现会出错。

  ─── 表头标准 ───────────────────────────────────────────────────

  固定 5 列：UI 元素 / API 字段 / 计算规则 / 数据源（服务端读取） / 配置源（服务端读取）
  列头中的"（服务端读取）"明确告知 AI：前端不直接访问这些表。

  表前必须有标准说明行：
  > 下表为端到端追踪：UI 元素 ← API 字段 ← 服务端计算 ← 数据源/配置源。
  > **数据源和配置源均由服务端读取**，前端不直接访问。

  ─── 何时需要计算输入表 ───────────────────────────────────────

  如果服务端计算依赖的中间变量（如 maintenanceCount）未出现在 API 响应中，
  已经在 X.2 服务端处理逻辑的「处理输入」表中覆盖，此处不再重复。

  ❌ 错误：数据源列写"服务端计算"
  ✅ 正确：精确到 `表名.字段名`，配置源列写 `配置表.字段`
-->

> 下表为端到端追踪：UI 元素 ← API 字段 ← 服务端计算 ← 数据源/配置源。**数据源和配置源均由服务端读取**，前端不直接访问。

| UI 元素 | API 字段 | 计算规则 | 数据源（服务端读取） | 配置源（服务端读取） |
|---------|----------|---------|--------|--------|
| {元素} | `{字段}` | {规则或"按 xxx 查配置"} | {`表.字段` 或 —} | {`配置表.字段` 或 —} |

### X.4 异常与兜底

<!--
  必须区分服务端兜底和前端渲染兜底，分两个子表格写。

  判断依据：
  - 服务端兜底：数据缺失/查询失败/计算结果异常 → 服务端返回默认值或空集合
  - 前端渲染兜底：API 字段为空/null、资源加载失败、协议不支持 → 前端降级渲染

  ❌ 错误：把服务端逻辑和前端逻辑混在一张表里
  ✅ 正确：两个独立的「服务端兜底」+「前端渲染兜底」子表格
-->

**服务端兜底**：

| 场景 | 处理 |
|------|------|
| {数据缺失/查询失败场景} | {返回默认值或空集合} |

**前端渲染兜底**：

| 场景 | 处理 |
|------|------|
| {API 字段为 null/空/资源加载失败场景} | {降级展示方式} |

### X.5 管理台配置：{配置页名称}

<!--
  如果该子区域有对应的管理台配置页，在这里描述。
  如果没有，删除此小节。

  ─── 必须包含 ────────────────────────────────────────
  1. 菜单名称（侧边栏中文显示名）
  2. 所属菜单（一级菜单 › 二级菜单）
  3. 路径
  4. 一句话功能说明
  5. 配置字段表（4列：字段 / 输入类型 / 约束 / 说明）
  6. 接口（GET + PUT）
  7. 现状差异（如有）
  8. 存储表名

  ─── 配置值的标注原则 ────────────────────────────────────────
  表格中出现的文案值/颜色值/数字值，统一标注为"初始值"而非硬编码规格。
  示例：
  > **注意**：上表中的值为 `xxx` 表的初始值，**不是硬编码**。
  > 运营可通过管理台随时修改。

  ❌ 错误：只写路径，不写菜单名称
  ✅ 正确：菜单名称 + 所属菜单 + 路径，三者都写
-->

**菜单名称**：{侧边栏显示的中文名}
**所属菜单**：{一级菜单} › {二级菜单（如有）}
**路径**：`/{route}`

{一句话说明配置页功能}

| 字段 | 输入类型 | 约束 | 说明 |
|------|---------|------|------|
| {字段} | {输入类型} | {必选/最多N字/NxNpx 等} | {说明} |

**接口**：
- `GET /api/admin/{path}` — 获取配置
- `PUT /api/admin/{path}` — 保存配置

**存储**：`{表名}` 表

---

## §Y {功能子区域2}

<!--
  重复上面的 5 子节结构：
  Y.1 用户体验（数据来源 + 布局 + 前端职责）
  Y.2 服务端处理逻辑（步骤 + 处理输入表）— 简单直透可省略
  Y.3 数据链路（5 列追溯表 + 表前说明）
  Y.4 异常与兜底（服务端兜底 + 前端渲染兜底）
  Y.5 管理台配置（如有）
-->

### Y.1 用户体验

### Y.2 服务端处理逻辑

### Y.3 数据链路

### Y.4 异常与兜底

---

<!--
  根据区块复杂度，继续添加功能子区域章节。
  复杂区块（如保养卡片区）可能有 8-10 个子区域。
  简单区块（如车辆选择区）可能只有 2 个。
-->

## §M 异常与兜底策略（全局）

<!--
  可选章节。当本区块的核心接口是聚合型（读多个数据源），需要此章节。
  简单区块（单数据源接口）可省略。

  ─── 包含两部分 ────────────────────────────────────────
  1. 接口级兜底：整个接口超时/报错时的处理
  2. 子数据源降级：单个数据源失败时，其他数据源不受影响

  ─── 与功能子区域兜底的分工 ────────────────────────────────
  功能子区域的 X.4 = 该子区域自己的字段级兜底
  本章节 = 跨子区域的全局降级策略
-->

### M.1 接口级兜底

| 场景 | 处理 |
|------|------|
| `{METHOD} /api/{path}` 超时 | {全局降级展示，如骨架屏 + 重试} |
| 接口返回 `code ≠ 0` | {同上} |

### M.2 子数据源降级

<!--
  聚合型接口（如 basic-info）读多个数据源。
  单一子数据源失败不应导致整个接口失败。
  每行描述：哪个数据源 → 失败影响哪些字段 → 降级为什么值。
-->

| 子数据源 | 失败影响 | 降级策略 |
|---------|---------|---------|
| `{数据源表}` | 无法计算 {字段} | {该字段返回默认值/空集合，其他字段正常} |

---

## §M+1 接口契约

<!--
  ==========================================
  接口契约写法规范
  ==========================================

  ─── 核心原则：引用 API PRD，不重复定义 ────────────────────

  完整的接口契约（请求参数、响应字段、响应示例、计算链路）
  定义在 prd-全局应用-api.md 中。子 PRD 的接口契约章节**引用**而非**复制**。

  子 PRD 只补充本区块特有的业务说明：
  1. 本区域使用的全部响应字段列表（按功能子区域标注章节）
  2. 前端渲染约束（如"前端不做业务判断"）
  3. 特殊矩阵/映射表（如 repairProgress 节点推进矩阵）

  ─── 如果 API PRD 尚未完成 ────────────────────────────────

  可先在子 PRD 中内联写完整契约（参数表 + 字段表 + 响应示例），
  待 API PRD 写好后再改为引用。

  ─── Admin API ────────────────────────────────────────────

  管理台 Admin API 已在各功能子区域的 X.5 管理台配置小节中定义，
  此处不再重复。如需汇总索引，用列表引用各子节即可。
-->

### M+1.1 C 端接口：`{METHOD} /api/{path}`

{一句话说明接口用途}

> 完整接口契约（请求参数、响应字段、响应示例、计算链路）见 [prd-全局应用-api §N.M](prd-全局应用-api.md#锚点)。

**本区域业务说明**：

- **本区域使用的全部响应字段**：`{字段1}`（§X）、`{字段2}`（§Y）、`{字段3[]}`（§Z）
- **前端不做业务判断**：{所有状态判定/文案拼装/匹配逻辑均由服务端完成，前端仅渲染返回值}

<!--
  如果有特殊矩阵/映射表（如节点推进状态矩阵），在此补充。
-->

<!--
  如果当前接口实现与目标态有偏差，加现状差异标注：
  > **现状差异**：{当前实现，附代码文件路径}。
  > 需要：{改动方向}。
-->

---

## §M+2 验收标准

<!--
  ==========================================
  验收标准写法规范
  ==========================================

  ─── 核心原则：与需求清单交叉对应 ────────────────────────────────

  §1 需求清单的每条需求（R1/R2/R3...）都必须有对应的验收用例。
  写完验收标准后，回看需求清单逐条核对，确保无遗漏。

  ─── 四类必覆盖场景 ────────────────────────────────────────────

  验收标准按功能分组，每个功能子区域至少覆盖以下 4 类场景
  （不是每个子区域都有全部 4 类，按实际内容裁剪）：

  1. **业务规则**：从 X.2 服务端处理逻辑 / X.3 数据链路提取
     - 每条核心规则（出现条件、消失条件、匹配规则）至少 1 条用例
     - 边界值场景（刚好满足/刚好不满足）优先覆盖
     - 表头：# / 场景 / 输入条件 / 预期结果

  2. **UX 交互**：从 X.1 用户体验的「交互」部分提取
     - 点击/关闭/切换/滚动等交互行为各 1 条
     - 互斥/联动等复合交互场景
     - 表头：# / 场景 / 操作 / 预期结果

  3. **管理台配置闭环**：从 X.5 管理台配置提取
     - 每种可编辑字段类型（文案/颜色/开关/数值/JSON）至少 1 条
     - 验证"管理台改 → C 端立即生效"的端到端闭环
     - 表头：# / 场景 / 操作 / 预期结果

  4. **异常兜底**：从 X.4 异常与兜底提取
     - 服务端兜底表的每条规则至少 1 条用例
     - 前端渲染兜底的关键场景（字段为空/null）
     - 表头：# / 场景 / 输入条件 / 预期结果

  ─── 边界约束 ────────────────────────────────────────────────

  - 只验收本区块的功能，不验收其他区块的行为
  - 反例：不要在车辆选择区验收"切换后保养状态正确刷新"
  - 序号全局递增，不按分组重置

  ❌ 错误：只写业务规则的 happy path，漏掉交互/闭环/异常
  ✅ 正确：4 类场景全覆盖，与需求清单交叉无遗漏
-->

### {业务规则组}

| # | 场景 | 输入条件 | 预期结果 |
|---|------|---------|----------|
| 1 | {正向场景} | {满足条件} | {预期结果} |
| 2 | {反向场景} | {不满足条件} | {预期结果} |

### {UX 交互组}

| # | 场景 | 操作 | 预期结果 |
|---|------|------|----------|
| N | {交互场景} | {用户操作} | {预期结果} |

### 管理台配置闭环

| # | 场景 | 操作 | 预期结果 |
|---|------|------|----------|
| N | {改某类配置} | 管理台修改 {字段} | C 端 {对应变化} |

### 异常兜底

| # | 场景 | 输入条件 | 预期结果 |
|---|------|---------|----------|
| N | {数据缺失/查询失败} | {异常条件} | {降级表现} |
